version: '3.1'

services:
  voteframe:
    env_file:
      - .env
    build:
      context: ./
      args: 
        - APP_URL=${VOCDONI_SERVER}
    ports:
      - 8888:8888
    sysctls:
      net.core.somaxconn: 8128
    volumes:
      - voteframe:/app/data
    restart: ${RESTART:-unless-stopped}

volumes:
  voteframe: {}
